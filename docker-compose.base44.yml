services:
  bot:
    image: node:22
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    command: sh -c "npm install && npx --yes nodemon index.js"
    restart: unless-stopped
